# Python over Go for implementation

The spec prescribed Go (single binary, cobra, bubbletea). We chose Python instead: faster iteration, richer ecosystem for agent-executor integration, team preference. The single-binary distribution advantage doesn't outweigh the velocity gain for an early-stage tool still finding its shape. Package via `pip` / `uv` tool.
